Patches in this folder are PR-ready text diffs. Apply with `git apply --index <patch>` from the repo root (inside full-bay-media/site).

- critical-css.patch  — adds a small critical CSS fallback to public/critical.css and guards for header + hero CTA
- cta-breakpoints.patch — adds CSS variables + utility class to ensure CTA safe spacing across breakpoints
- a11y-microfixes.patch — small ARIA improvement for the hero CTA
