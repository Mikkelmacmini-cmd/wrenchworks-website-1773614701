WrenchWorks preview build notes

What to expect:
- index.html (hero + lead form)
- contact.html (simple form placeholder)
- city-highlands-ranch.html (city landing template)

Next steps to finalize before publish:
1. Swap in real hero images when provided (replace mockups in /Images)
2. Wire up real contact form backend (SMTP or form endpoint)
3. Replace placeholder map with real Google Maps embed and add LocalBusiness schema
4. Run Lighthouse and optimize images (AVIF) and critical CSS

Paths:
- /Users/openclaw/.openclaw/workspace/projects/wrenchworks-preview/
